[build-system]
requires = ["setuptools",
    "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["version", "dependencies", "optional-dependencies"]
name = "pyvista_tools"
[tool.setuptools_scm]